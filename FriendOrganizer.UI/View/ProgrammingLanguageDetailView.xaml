<UserControl x:Class="FriendOrganizer.UI.View.ProgrammingLanguageDetailView"
						 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
						 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
						 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
						 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
						 xmlns:local="clr-namespace:FriendOrganizer.UI.View"
						 mc:Ignorable="d"
						 d:DesignHeight="450" d:DesignWidth="800">
	<Grid >
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
			<ColumnDefinition Width="Auto"/>
		</Grid.ColumnDefinitions>
		<DataGrid ItemsSource="{Binding ProgrammingLanguages}"
							SelectedItem="{Binding SelectedProgrammingLanguage,Mode=TwoWay}"
							AutoGenerateColumns="False"
							RowHeaderWidth="0"
							Margin="10"
							>
			<DataGrid.Columns>
				<DataGridTextColumn
					Header="Programming Language" Width="*"
					ElementStyle="{StaticResource DataGriColumnStyle}"
					EditingElementStyle="{StaticResource DataGridEitingColumnStyle}"
					Binding="{Binding Name,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
					/>
			</DataGrid.Columns>
		</DataGrid>
		<StackPanel Grid.Column="0" Grid.Row="1">
			<Button Content="Save" Command="{Binding SaveCommand}"/>
		</StackPanel>

		<StackPanel Grid.Column="1" Grid.Row="0" Margin="0 5 5 5">
			<Button Content="Add" Margin="5" Width="125" Command="{Binding AddCommand}"/>
			<Button Content="Remove" Margin="5" Width="125" Command="{Binding RemoveCommand}"/>
		</StackPanel>
	</Grid>
</UserControl>